#ifndef ENUMS
#define ENUMS

enum LvlOfEdu { Bachelor = 1, Master = 2, Postgraduate = 3, UNDEFL = -1 };
enum SylStatus { Active = 1, Inactive = -1 };
enum StatusInUni { Lecturer = 1, Professor, Dean, ViceRector, Rector, UNDEFSIN = -1 };

#endif 
